# 2주차 보고서

### 1. 상품코드를 입력하면 즉시 재고 수량을 확인해야 하는 재고 관리 데이터

#### 선택한 자료구조 : 딕셔너리

선택 근거 : 상품코드를 키 값으로 하여 상품의 재고 수를 벨류로 유지하면 접근시간을 다른 자료구조에 비해 크게 줄일 수 있어 선택한다. 

튜플은 재고 수를 수정해야 하므로 사용할 수 없고 리스트의 경우에는 상품코드가 일치할 때 까지 탐색해야 하므로 최악의 경우 상품의 수 만큼 순회해야한다. 그러므로 비효율적이므로 딕셔너리를 선택한다.

예시코드

```python
# 출력 결과 // 상품코드: item_328423 상품 재고: 12개
items = {"item_248231" : 14, "item_328423" : 12, "item_213912" : 0}
search_item = "item_328423"
current_stock = items[search_item]
print(f"상품코드: {search_item} 상품 재고: {current_stock}개")
```

### 2. 이벤트 응모 고객 명단에서 중복 응모를 제거하고, 기존 회원 명단과의 교집합을 구해야 하는 상황

#### 선택한 자료구조 : 셋

선택 근거 : 중복을 간단하게 없애고 교집합 합집합을 쉽게 구할 수 있는 셋을 사용하는게 편리하다. 다른 자료구조를 사용할 경우 중복검사를 하는 코드를 별도로 작성해야 하고 효율이 떨어진다.

예시코드

```python
# 출력 결과 // {'노진구', '나철수'}
vote_list = set(["홍길동","나철수","노진구","나철수"])
club_list = set(["나철수", "노진구","김구라"])
final_list = vote_list & club_list
print(final_list)
```

### 3. 월별 매출액을 1월부터 12월까지 순서대로 저장하고 순회하는 데이터

#### 선택한 자료구조 : 리스트, 튜플

선택 근거 : 1~12월은 인덱스 번호로 쉽게 관리가 가능하며 번거로운 딕셔너리의 해시 테이블 로직을 사용하지 않아도 된다. 그러므로 자료가 수정될 가능성이 있으면 리스트 아니면 튜플을 고르는 것이 합리적이다.

예시코드

```python
monthly_sales = ( # 인덱스 번호와 월 수를 일치시키기 위해 인덱스 0에 0이라는 더미데이터를 넣는다
    0, 10000, 11000, 12000, 13000, 24000, 25000,
    30000, 29000, 22000, 20000, 19000, 18000
)
print(monthly_sales[1])  # 1월 매출액 출력
```

### 4. 한 번 발급되면 절대 변경되어서는 안 되는 (위도, 경도) 매장 좌표

#### 선택한 자료구조 : 튜플

선택 근거 : 변경이 되면 안된다는 조건이 있어 튜플을 사용하는 것이 적합하다. 다른 자료구조는 변경이 가능함으로 취약성이 있다.

예시코드

```python
# 한번 지정된 좌표를 강제로 변경하려고 시도시 오류 메세지가 출력된다 // TypeError: 'tuple' object does not support item assignment
store_axis = (123.324234,36.324234)
store_axis[0] = 0
```

### 5. 1,000만 줄짜리 웹 서버 접속 로그 파일에서 특정 조건의 줄 수를 세는 작업 (자료구조 + 처리 방식 관점에서 서술)

#### 선택한 자료구조 : 특정 조건의 줄 수를 세는 변수(int)

선택 근거 : 1000만줄의 데이터를 특정 자료구조에 저장하려고 한다면 막대한 양의 메모리가 필요해진다. 그러므로 모든 줄을 저장하는 것이 아니라
각 줄의 조건을 확인하는 코드에 조건만족 줄을 카운트하는 변수만을 유지하여 공간복잡도를 줄이는 방안을 선택하는 것이 좋다.

예시코드

```python
log = open("server_log.txt", "r")  #로그 파일 열기
count = 0  # int형 줄 수 카운터
while True:
    line = log.readline()  # 한 줄씩 읽기
    if not line:  # 파일 끝에 도달하면 종료
        break
    if "특정 조건" in line:  # 특정 조건을 만족하는지 확인
        count += 1  # 조건 만족 시 카운트 증가
print(f"특정 조건을 만족하는 줄 수: {count}")  # 결과 출력
```